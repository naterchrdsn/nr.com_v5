---
---

<div class="about-dialog win95-text">
  <!-- Tab Bar -->
  <div class="about-tabs">
    <button class="win95-tab active" data-about-tab="general">General</button>
    <button class="win95-tab" data-about-tab="personal">Personal</button>
    <button class="win95-tab" data-about-tab="skills">Skills</button>
  </div>

  <!-- Tab Content Area (raised panel that sits behind the active tab) -->
  <div class="about-tab-body win95-raised">

    <!-- General Tab -->
    <div class="about-tab-panel active" data-about-panel="general">
      <div class="about-general-layout">
        <!-- Left: Headshot in sunken frame -->
        <div class="about-photo-area">
          <div class="about-photo-frame win95-sunken">
            <img
              src="/images/nate_richardson_headshot2.jpg"
              alt="Nate Richardson"
              class="about-photo"
              loading="lazy"
              decoding="async"
            />
          </div>
        </div>

        <!-- Right: Identity info -->
        <div class="about-identity">
          <fieldset class="about-groupbox">
            <legend>User Profile</legend>
            <div class="about-prop-row">
              <span class="about-prop-label">Name:</span>
              <span class="about-prop-value win95-sunken">Nate Richardson</span>
            </div>
            <div class="about-prop-row">
              <span class="about-prop-label">Title:</span>
              <span class="about-prop-value win95-sunken">Frontend Architect & Developer</span>
            </div>
            <div class="about-prop-row">
              <span class="about-prop-label">Company:</span>
              <span class="about-prop-value win95-sunken">Nacho Labs / Deposco</span>
            </div>
            <div class="about-prop-row">
              <span class="about-prop-label">Location:</span>
              <span class="about-prop-value win95-sunken">Atlanta, GA</span>
            </div>
          </fieldset>
        </div>
      </div>

      <!-- Tagline in a sunken status area -->
      <div class="about-tagline win95-sunken">
        I believe great software (like great food) should be crafted with care, built to last, and enjoyed by the people using it.
      </div>
    </div>

    <!-- Personal Tab -->
    <div class="about-tab-panel" data-about-panel="personal">
      <fieldset class="about-groupbox">
        <legend>About Me</legend>
        <div class="about-bio-scroll win95-sunken win95-scrollable">
          <p class="about-bio-p">Hey there, I'm Nate!</p>
          <p class="about-bio-p">
            I own and operate Nacho Labs, a boutique web solutions company specializing in development, hosting, and webmastering services in the Atlanta area. I also work full-time as a Senior UI Engineer at Deposco, where I contribute to enterprise supply chain management solutions. When I'm not coding, I'm usually tinkering&mdash;whether with my home lab or a grill full of something delicious.
          </p>
          <p class="about-bio-p">
            I enjoy video games, spending time with my two dogs and three cats, and generally finding new things to make, break, and improve.
          </p>
          <p class="about-bio-p last">
            If you want to collaborate, swap ideas, or just talk shop over a good cup of mushroom coffee, drop me a line. Let's build something great.
          </p>
        </div>
      </fieldset>

      <fieldset class="about-groupbox">
        <legend>Properties</legend>
        <div class="about-properties">
          <div class="about-prop-row">
            <span class="about-prop-label">Fuel:</span>
            <span class="about-prop-value win95-sunken">Mushroom Coffee</span>
          </div>
          <div class="about-prop-row">
            <span class="about-prop-label">Pets:</span>
            <span class="about-prop-value win95-sunken">2 Dogs, 3 Cats</span>
          </div>
          <div class="about-prop-row">
            <span class="about-prop-label">Hobbies:</span>
            <span class="about-prop-value win95-sunken">Homelab, Grilling, Video Games</span>
          </div>
          <div class="about-prop-row">
            <span class="about-prop-label">Status:</span>
            <span class="about-prop-value win95-sunken">Making, breaking, improving</span>
          </div>
        </div>
      </fieldset>
    </div>

    <!-- Skills Tab -->
    <div class="about-tab-panel" data-about-panel="skills">
      <fieldset class="about-groupbox">
        <legend>Frontend</legend>
        <div class="about-skills-grid">
          <div class="about-skill-item">
            <div class="about-skill-icon">TS</div>
            <span>TypeScript</span>
          </div>
          <div class="about-skill-item">
            <div class="about-skill-icon">JS</div>
            <span>JavaScript</span>
          </div>
          <div class="about-skill-item">
            <div class="about-skill-icon">Re</div>
            <span>React</span>
          </div>
          <div class="about-skill-item">
            <div class="about-skill-icon">Vw</div>
            <span>Vue</span>
          </div>
          <div class="about-skill-item">
            <div class="about-skill-icon">As</div>
            <span>Astro</span>
          </div>
          <div class="about-skill-item">
            <div class="about-skill-icon">Nx</div>
            <span>Next.js</span>
          </div>
          <div class="about-skill-item">
            <div class="about-skill-icon">Tw</div>
            <span>Tailwind</span>
          </div>
          <div class="about-skill-item">
            <div class="about-skill-icon">CS</div>
            <span>CSS</span>
          </div>
        </div>
      </fieldset>

      <fieldset class="about-groupbox">
        <legend>Backend & DevOps</legend>
        <div class="about-skills-grid">
          <div class="about-skill-item">
            <div class="about-skill-icon">No</div>
            <span>Node.js</span>
          </div>
          <div class="about-skill-item">
            <div class="about-skill-icon">Py</div>
            <span>Python</span>
          </div>
          <div class="about-skill-item">
            <div class="about-skill-icon">Dk</div>
            <span>Docker</span>
          </div>
          <div class="about-skill-item">
            <div class="about-skill-icon">Li</div>
            <span>Linux</span>
          </div>
          <div class="about-skill-item">
            <div class="about-skill-icon">Gm</div>
            <span>Git</span>
          </div>
          <div class="about-skill-item">
            <div class="about-skill-icon">CI</div>
            <span>CI/CD</span>
          </div>
        </div>
      </fieldset>
    </div>

  </div>
</div>

<style>
  .about-dialog {
    background: var(--win95-grey);
    padding: 8px 8px 6px 8px;
    height: 100%;
    display: flex;
    flex-direction: column;
  }

  /* --- Tab Bar --- */
  .about-tabs {
    display: flex;
    gap: 0;
    padding-left: 4px;
    position: relative;
    z-index: 2;
  }

  .about-tabs .win95-tab {
    padding: 2px 12px;
    margin-right: 2px;
    font-size: 11px;
    position: relative;
    top: 2px;
    background: var(--win95-grey);
    border: 2px solid;
    border-color: var(--win95-white) var(--win95-shadow-dark) var(--win95-shadow-dark) var(--win95-white);
    border-bottom: none;
    cursor: pointer;
    font-family: inherit;
  }

  .about-tabs .win95-tab.active {
    background: var(--win95-grey);
    border-bottom: 2px solid var(--win95-grey);
    z-index: 3;
    padding-top: 2px;
    margin-top: -2px;
  }

  /* --- Tab Body (the raised panel beneath the tabs) --- */
  .about-tab-body {
    flex: 1;
    padding: 12px;
    position: relative;
    z-index: 1;
    overflow-y: auto;
    display: flex;
    flex-direction: column;
  }

  /* --- Tab Panels --- */
  .about-tab-panel {
    display: none;
    flex-direction: column;
    gap: 8px;
    flex: 1;
  }

  .about-tab-panel.active {
    display: flex;
  }

  /* --- General Tab Layout --- */
  .about-general-layout {
    display: flex;
    gap: 12px;
    align-items: flex-start;
  }

  .about-photo-area {
    flex-shrink: 0;
  }

  .about-photo-frame {
    padding: 3px;
    width: 108px;
    height: 108px;
  }

  .about-photo {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
    image-rendering: auto;
  }

  .about-identity {
    flex: 1;
    min-width: 0;
  }

  /* --- Tagline bar at bottom of General tab --- */
  .about-tagline {
    padding: 6px 8px;
    font-style: italic;
    color: var(--win95-darkgrey);
    font-size: 11px;
    line-height: 1.4;
    margin-top: auto;
  }

  /* --- Groupbox (fieldset) --- */
  .about-groupbox {
    border: 2px solid;
    border-color: var(--win95-darkgrey) var(--win95-white) var(--win95-white) var(--win95-darkgrey);
    padding: 8px 10px 8px 10px;
    margin: 0;
  }

  .about-groupbox legend {
    font-size: 11px;
    padding: 0 4px;
    color: var(--win95-black);
  }

  /* --- Property Rows (label: value) --- */
  .about-prop-row {
    display: flex;
    align-items: center;
    gap: 6px;
    margin-bottom: 4px;
  }

  .about-prop-row:last-child {
    margin-bottom: 0;
  }

  .about-prop-label {
    font-size: 11px;
    color: var(--win95-black);
    white-space: nowrap;
    min-width: 62px;
    text-align: right;
  }

  .about-prop-value {
    flex: 1;
    font-size: 11px;
    padding: 1px 4px;
    min-height: 18px;
    display: flex;
    align-items: center;
    background: var(--win95-white);
  }

  /* --- Bio scroll area in Personal tab --- */
  .about-bio-scroll {
    padding: 6px 8px;
    max-height: 140px;
    overflow-y: auto;
    background: var(--win95-white);
  }

  .about-bio-p {
    margin: 0 0 8px 0;
    line-height: 1.5;
    font-size: 11px;
  }

  .about-bio-p.last {
    margin-bottom: 0;
  }

  /* --- Properties section in Personal tab --- */
  .about-properties {
    display: flex;
    flex-direction: column;
    gap: 0;
  }

  /* --- Skills Grid --- */
  .about-skills-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(90px, 1fr));
    gap: 6px;
  }

  .about-skill-item {
    display: flex;
    align-items: center;
    gap: 6px;
    padding: 2px 4px;
    font-size: 11px;
    cursor: default;
  }

  .about-skill-item:hover {
    background: var(--win95-navy);
    color: var(--win95-white);
  }

  .about-skill-icon {
    width: 24px;
    height: 24px;
    background: var(--win95-white);
    border: 2px solid;
    border-color: var(--win95-darkgrey) var(--win95-white) var(--win95-white) var(--win95-darkgrey);
    box-shadow: inset 1px 1px 0 var(--win95-black), inset -1px -1px 0 var(--win95-shadow-light);
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 9px;
    font-weight: bold;
    color: var(--win95-navy);
    flex-shrink: 0;
  }

  .about-skill-item:hover .about-skill-icon {
    color: var(--win95-navy);
  }

  /* --- Responsive tweaks for mobile --- */
  @media (max-width: 480px) {
    .about-general-layout {
      flex-direction: column;
      align-items: center;
    }

    .about-identity {
      width: 100%;
    }

    .about-prop-label {
      min-width: 52px;
    }

    .about-skills-grid {
      grid-template-columns: repeat(auto-fill, minmax(80px, 1fr));
    }
  }
</style>

<script>
  function initAboutTabs() {
    const tabs = document.querySelectorAll<HTMLElement>('[data-about-tab]');
    const panels = document.querySelectorAll<HTMLElement>('[data-about-panel]');

    tabs.forEach((tab) => {
      tab.addEventListener('click', () => {
        const target = tab.dataset.aboutTab;

        // Deactivate all tabs and panels
        tabs.forEach((t) => t.classList.remove('active'));
        panels.forEach((p) => p.classList.remove('active'));

        // Activate clicked tab and matching panel
        tab.classList.add('active');
        const panel = document.querySelector<HTMLElement>(`[data-about-panel="${target}"]`);
        if (panel) panel.classList.add('active');
      });
    });
  }

  // Run on load and also on Astro page transitions
  initAboutTabs();
  document.addEventListener('astro:after-swap', initAboutTabs);
</script>
